The file contains two python functions: the first (Ngap_position_count) to count the number of gaps or Ns in every position of a multiple sequence alignment in fasta format and the second (save_as_df) to save the result as a data frame in a tsv file. 
